{
  "name": "shuoshulou",
  "private": true,
  "version": "0.1.0",
  "description": "说书楼 · 多小说写作与阅读平台（写作走 AI 生成器流程，阅读走网页）",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "prebuild": "node scripts/sync.mjs",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "marked": "^12.0.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.8"
  }
}
